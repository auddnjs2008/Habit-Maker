import styled from '@emotion/styled';

export const Container = styled.div`
  position: absolute;
  top: 0;
  left: 0;
  transform: transalte(-50%, -50%);
  border: 1px solid black;
  height: 100vh;
`;

export const WeekPlanUL = styled.ul`
  display: flex;
`;
